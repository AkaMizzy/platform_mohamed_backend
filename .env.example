PORT=5000
NODE_ENV=development

# MySQL (XAMPP defaults)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lafrikhi_platform

# Comma-separated list of origins allowed to call this API
CORS_ORIGIN=http://localhost:5173

# JWT auth (generate your own random secret for each environment)
JWT_SECRET=changeme
JWT_EXPIRES_IN=7d
